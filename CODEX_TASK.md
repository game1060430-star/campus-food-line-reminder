# Codex 開發任務 — 請直接開始實作

你正在接手一個已經有 FastAPI MVP 的專案。不要只產生規格或 mockup；請在此 repo 直接實作、執行測試、修正錯誤，直到可用的第一個完整版本。

## 使用者情境
使用者幾乎只用手機。LINE 官方帳號是主要入口；複雜表單以 LIFF 頁面呈現。系統支援多分店，每間分店可有不同的校園食材登錄平台帳號。

平常每天菜單多半固定。使用者設定營業日與預設菜單後，系統應自動維持未來日期的資料並自動建立上傳任務。若要改菜單，使用者可以提前一天或提前一段時間選日期/日期區間修改；若新增菜色，可立即綁食材與調味料，並選擇要套用的日期。

## 不可改變的產品原則
1. **極簡輸入**：只有官方必要欄位強制。非必要欄位預設隱藏/留空，使用者主動展開或勾選才填。
2. **多分店**：分店有自己的學校、供餐地點、餐廳名稱、營業日、官方帳號/密碼、菜單排程與上傳狀態。
3. **LINE first**：日常操作都能從 LINE OA 進入；複雜多選/月曆/編輯用 LIFF。
4. **變更驅動上傳**：只重新處理受影響日期，不要每次把全部歷史資料重傳。
5. **歷史不可被主檔改寫**：供應商或食材日後變更時，過去已上傳/已確認的資料必須保留當時快照。
6. **官方 Excel 樣板為唯一真相來源**：輸出要讀 template 寫入，不可新建一份看起來一樣的 xlsx。
7. **驗證碼真人接管**：若登入遇 CAPTCHA，截圖後送至 LINE/LIFF 等待真人輸入，收到答案後續跑同一任務。不要 OCR 或破解 CAPTCHA。
8. **成功必須可證明**：上傳後要讀官方網站成功訊息/結果並寫 audit log；若不能確認，狀態不得標 success。

## 先做的工作（依順序）

### Phase 1 — 資料層 + 既有 Excel 匯入
- 完成 SQLAlchemy models/migrations。
- 實作供應商、食材主檔 + 食材進貨批次、調味料主檔 + 使用批次、菜色 + 配方關聯、每日菜單、分店、營業日、UploadTask、UploadAttempt、CaptchaChallenge、Snapshot/Audit。
- 匯入 `data/current/` 4 份現有資料，避免重複資料。
- 寫 import dry-run 報告：新增/合併/略過/有疑義筆數。

### Phase 2 — 手機優先管理介面
- 分店管理。
- 食材/調味料/供應商 CRUD；非必要欄位收在「更多資料」。
- 菜色新增/編輯：多選食材與調味料，可在同畫面快速新增缺少的主檔。
- 日曆/日期區間排程：單日、多日、星期幾、營業日、複製昨天/上週同日。
- 菜單異動後自動標出受影響日期並建立 upload task。

### Phase 3 — Excel Export Engine
- 依 `data/templates/` 4 份官方樣板輸出。
- 保留工作表名稱、參考工作表、資料驗證、格式與必要結構。
- 僅填必要資料與使用者已啟用的選填資料。
- 匯出前 validation center 顯示可點擊修正的錯誤。
- 支援單日與日期區間。
- 寫 round-trip/structure tests，確認 template 結構沒有被破壞。

### Phase 4 — LINE Messaging API + LIFF
- 驗證 `X-Line-Signature`。
- Rich menu/按鈕入口：今日狀態、菜單、新增菜色、進貨、食材/調味料、上傳狀態。
- LIFF 手機頁面沿用 web 功能，不維護兩套商業邏輯。
- LINE User ID 綁定角色/權限。
- 重要變更採「確認後執行」，自然語言解析不得直接寫正式資料。

### Phase 5 — 官方網站瀏覽器自動化
- 使用 Playwright。
- **先人工/程式探索官方網站實際 DOM，禁止猜 selector。**
- 分店登入 session 隔離保存。
- Session 有效則直接續用；失效才重新登入。
- CAPTCHA：只截取圖片並建立 waiting_captcha；發 LINE 通知/LIFF link；真人輸入後重新提交；若刷新 CAPTCHA，舊 challenge 失效。
- 自動上傳 Excel 並讀取官方成功/失敗結果。
- 可重試但不可造成重複上傳；以 idempotency/task key 控制。

### Phase 6 — 排程與防漏登錄
- 每間分店設定星期一～日營業日。
- 可設定預設菜單模板並自動維持未來 N 天（預設 30 天）預排菜單。
- 每日檢查應登錄但未成功的任務。
- 平常成功可靜默；只在異常、等待 CAPTCHA、資料缺漏時通知；可提供每日一次總結。

## 第一版完成定義
完成下列情境才算 done：
1. 新增一間分店，設定週一～週五營業。
2. 從現有 Excel 匯入供應商/食材/調味料/菜色資料。
3. 新增菜色，選擇多個食材與調味料。
4. 將菜色套用到一段日期內的週一～週五。
5. 改其中一天的菜單，只該日變成待更新。
6. 正確產出 4 種官方 Excel，必要欄位齊全、非必要欄位可空白。
7. 模擬登入流程遇到 CAPTCHA 時任務停在 waiting_captcha，手機頁輸入答案後續跑。
8. 模擬上傳成功/失敗，LINE/狀態中心可明確看到結果。
9. 單元測試與整合測試可執行，README 有本機啟動與部署說明。

## 開發決策
- 現有 FastAPI + SQLAlchemy 可以保留。
- DB 開發期可 SQLite；結構需可切 PostgreSQL。
- 前端可先 server-rendered + HTMX/Alpine/Tailwind 或現有 Jinja；手機 UX 比技術棧更重要。
- 背景任務建議使用 APScheduler/ARQ/Celery 任一合理方案；第一版可先以持久化 DB queue + worker 實現。
- Excel 實作優先使用可安全保留模板格式的 Python 套件；測試必須驗證 worksheet/validation/reference sheet 不被破壞。

## 不要做
- 不要用 OCR 解驗證碼。
- 不要把密碼放 LINE、log、HTML source 或 Git。
- 不要要求使用者把每個非必要官方欄位都填完。
- 不要在主檔更新時回寫歷史 snapshot。
- 不要在沒有官方成功證據時將任務標記為成功。
- 不要為了 demo 寫死目前輔仁大學資料；必須支援任意分店。

完成每個 phase 後都執行測試，直接修到通過，再進下一 phase。
